- display unmanaged servers
- display server info (settings, help text, etc.)

- update node settings in UI (directories, extensions, packages)
- add registry editing capabilities (use the manager to parse data)
  - items to add set new keys/dirs and delete old keys/dirs

- use https

- add wiring editor

- streamline deployment
- visual indication when connection to LabRAD is lost
- renegotiate connection automatically after reconnect
- update credentials on the fly from the registry

- access node settings remotely (e.g. directories, extensions)

- run multiple server instances on one node (set per-server environment variables, for example)

- log when servers start, stop, or fail

